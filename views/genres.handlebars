<div>
    <div>
        <h2>
            Genres
        </h2>
    </div>
    {{#each genre as |genre|}}
        <div>
            <div>
                <button>
                    {{id}}.
                    {{genre_name}}
                </button>
                <button>
                    {{id}}.
                    {{genre_name}}
                </button>
                <button>
                    {{id}}.
                    {{genre_name}}
                </button>
                <button>
                    {{id}}.
                    {{genre_name}}
                </button>
                <button>
                    {{id}}.
                    {{genre_name}}
                </button>
                
            </div>
            
        </div>
    {{/each}}
</div>
