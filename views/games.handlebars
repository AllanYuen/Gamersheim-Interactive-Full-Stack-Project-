{{#each games as |game|}}
<section class="card">

  <img src="/images/{{cover_image}}" alt="game cover">
  <div class="painting-details">
    <a href="{{features}}"target="_blank"><h3 style="color: blue;">{{title}}</h3></a>
    <p>{{game_description}}</p>    
    <p>Publisher: {{publisher}}</p>

  </div>
</section>
  {{/each}}